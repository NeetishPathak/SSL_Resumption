#generate an RSA key
openssl genrsa

#generate a 2048 bit RSA key and save it in a file mykey.pem
openssl genrsa -out mykey.pem 2048

#geerate a passkey protected rsa key
openssl genrsa -des3 -out mykey.pem 2048 
